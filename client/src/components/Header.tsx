import styles from '@/styles/components/Header.module.scss'

export const Header = () => {
  return (
    <header className={styles.header}>
      <h1 className={styles.title}>スタンプラリー</h1>
    </header>
  )
}
